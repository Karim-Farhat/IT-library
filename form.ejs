<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book Notes</title>
    <link rel="stylesheet" href="styles/main.css" />
  </head>

  <body>
    <header class="page-header">
      <h1><%= isEdit ? 'Edit Book' : 'Add New Book' %></h1>
      <p>
        <%= isEdit ? `Edit an existing book in your collection` : `Add a newbook
        to your collection` %>
      </p>
    </header>

    <main>
      <section class="form-container">
        <h2>Add a Book</h2>
        <br />
        <form action="<%= isEdit ? '/edit' : '/add' %>" method="POST">
          <input type="text" name="name" placeholder="Book title" required />
          <input type="text" name="author" placeholder="Author" required />
          <input
            type="float"
            name="rating"
            min="1"
            max="5"
            placeholder="Rating (1–5)"
            required
          />
          <textarea name="notes" placeholder="Your notes" required></textarea>

          <button type="submit">
            <%= isEdit ? 'Update Book' : 'Add Book' %>
          </button>
        </form>
      </section>
    </main>

    <footer class="page-footer">
      © <%= new Date().getFullYear() %> Book Notes
    </footer>
  </body>
</html>
